env:
  LOG_LEVEL: info
routes:
  gateway:
    namespace: unique
  pathPrefix: /speech
  paths:
    default:
      enabled: true
    probe:
      enabled: true
prometheus:
  enabled: false
serviceAccount:
  enabled: true
  workloadIdentity:
    enabled: true
