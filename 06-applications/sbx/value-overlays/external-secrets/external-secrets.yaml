# External Secrets Operator Helm values for AWS EKS
installCRDs: true

# ServiceAccount only - ClusterSecretStore deployed separately after CRDs are ready
serviceAccount:
  create: true
  name: external-secrets
  annotations: {}
