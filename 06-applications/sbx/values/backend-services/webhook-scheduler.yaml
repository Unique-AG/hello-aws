# Webhook Scheduler backend service configuration (AWS sbx)

image:
  repository: <ECR_REGISTRY_THIRDPARTY>/backend-service-webhook-scheduler
  tag: "2026.14.1"

extraEnvSecrets:
  - webhook-scheduler-secrets
