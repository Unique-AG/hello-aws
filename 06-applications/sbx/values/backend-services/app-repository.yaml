# App Repository backend service configuration (AWS sbx)

image:
  repository: <ECR_REGISTRY_THIRDPARTY>/backend-service-app-repository
  tag: "2026.14.1"

routes:
  hostname: <DOMAIN_API>

serviceAccount:
  enabled: true

extraEnvSecrets:
  - app-repository-secrets
