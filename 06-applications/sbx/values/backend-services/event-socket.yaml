# Event Socket backend service configuration (AWS sbx)

image:
  repository: 217381566492.dkr.ecr.eu-central-2.amazonaws.com/uniquecr/backend-service-event-socket
  tag: "2026.22.0"

routes:
  gateway:
    namespace: unique
  hostname: api.hello.aws.unique.dev
  pathPrefix: /event-socket
  paths:
    versioned:
      enabled: true
    probe:
      enabled: true
