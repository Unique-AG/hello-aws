# Builds Sandbox pods from the SandboxTemplate sbx-templates ships. 0.4.6-rc.1
# or newer: the 0.4.5 CRD lacks SandboxTemplate.service and prunes it. In the
# system group so its CRDs precede the conduct group.
spec:
  name: agent-sandbox-controller
  autoSync: false
  sources:
    - repoURL: oci://ghcr.io/unique-ag/helm-charts/agent-sandbox-controller
      chart: "."
      targetRevision: sha256:2eca7f113777f44129562cdcfaaac2d2132496c8b338d0ae5e0b0c62fdc9f11e
      helm:
        releaseName: agent-sandbox-controller
