# Backend configuration for {{ENV}} environment
# This file is automatically generated/updated by bootstrap script
#
# To use this backend config:
# terraform init -backend-config=environments/{{ENV}}/backend-config.hcl

bucket        = "{{BUCKET}}"
key           = "{{KEY}}"
region        = "{{REGION}}"
encrypt       = true
kms_key_id    = "{{KMS_ALIAS}}"
use_lockfile  = true
